<nav class="navbar navbar-default">
  <div class="container-fluid" id="container-fluid-1">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="nav-brand" href="<%= static_home_path() %>"><img src="/images/logo.jpg" alt="Asset logo" /></a>

    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
        <li><a href="#" id="white-font"><%= current_user.email %></a></li>
        <% if current_user.role == 3 %>
          <li>
            <a href="<%= static_board_path %>" id="white-font">
              <i class="fa fa-search" aria-hidden="true"></i> Buscar Tutorias
            </a>
          </li>
          <li>
            <a href="<%= tutor_path(current_user.user_role_id) %>" id="white-font">
              <i class="fa fa-calendar" aria-hidden="true"></i> Mi Calendario
            </a>
          </li>
          <li>
            <a href="<%= account_tutor_path(current_user.user_role_id) %>" id="white-font" >
              <i class="fa fa-tachometer" aria-hidden="true"></i> Mis Horas
            </a>
          </li>

        <% elsif current_user.role == 1 %>
          <li><a href="#" data-toggle="modal" data-target="#contact_modal" id="white-font" >Servicio al Cliente</a></li>
          <li><a href="<%= parent_calendar_parent_path(current_user.user_role_id)%>" id="white-font">Calendario</a></li>
          <li><a href="#" id="white-font">Perfil</a></li>
        <% end %>

        <li ><%=link_to "Salir", destroy_user_session_path, method: :delete, :id=>"white-font"%></li>
        <% end %>

      </ul>

    </div>


  </div>
</nav>
